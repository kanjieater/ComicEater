// set series root level
//

function convertArchiveToSeries(archivePath) {

}

function convertToSeries(archivesPaths) {
  archivesPaths.forEach((archivePath) => {
    convertArchiveToSeries();
  });
  // const isFile = await checkIsFile(startPath);
}

module.exports = {
  convertToSeries,
};
